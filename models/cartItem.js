class CartItem {
    constructor(qnt, price, title, pushToken, sum,image){
        this.quantity = qnt;
        this.price = price;
        this.title = title;
        this.pushToken = pushToken;
        this.sum = sum;
         this.image = image;
    }
} 

export default CartItem; 